/**
 * Flight Search Agent - Usage Example
 * 
 * DEPRECATED: Legacy OneAgent SDK example. See Gauss agents for current approach.
 */

import type { FlightSearchInput, FlightSearchOutput } from './schema';

/**
 * @deprecated Legacy SDK example — use Gauss flight agent instead.
 */
async function searchFlightsExample(): Promise<void> {
  throw new Error('Legacy SDK example removed. Use Gauss flight agent.');
}

searchFlightsExample().catch(console.error);
