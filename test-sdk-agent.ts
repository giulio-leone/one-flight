#!/usr/bin/env tsx
/**
 * DEPRECATED: Legacy OneAgent SDK 3.0 test.
 * Flight search now uses Gauss agents.
 */

async function testFlightSearch() {
  console.log('⚠️ Legacy SDK test deprecated. Use Gauss flight agent tests instead.');
  process.exit(0);
}

testFlightSearch().catch(console.error);
